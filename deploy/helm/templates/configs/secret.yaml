apiVersion: v1
kind: Secret
metadata:
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation
  name: {{ include "application.fullname" . }}
type: Opaque
data:
  DJANGO_SECRET_KEY: cGFzc3dvcmQ=
